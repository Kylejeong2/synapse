// Test file with intentionally bad formatting
export function testFunction() {
	const badSpacing = "no spaces";
	const badIndentation = "mixed quotes";
	return badSpacing + badIndentation;
}

// Missing semicolons and inconsistent spacing
const obj = { a: 1, b: 2, c: 3 };
const arr = [1, 2, 3];
